import * as home from './home';
import * as feed from './feed';
import * as notifications from './notifications';
import * as settings from './settings';

export { home, feed, notifications, settings };
